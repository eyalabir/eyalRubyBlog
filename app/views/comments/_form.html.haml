= form_for([@post, @post.comments.build], :html => {:id => 'create_comment_form'}, :remote => true) do |f|
  .field
    = f.label :commenter
    %br/
    = f.text_field :commenter
  .field
    = f.label :body
    %br/
    = f.text_area :body
  .actions
    = f.submit